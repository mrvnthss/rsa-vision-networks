num_classes: 10
stats:
  mean: [0.5, 0.5, 0.5]
  std: [0.5, 0.5, 0.5]
train_set:
  _target_: datasets.CIFAR10
  root: ${paths.data}
  train: True
  transform: ${model.preprocessing}
val_set:
  _target_: datasets.CIFAR10
  root: ${paths.data}
  train: False
  transform: ${model.preprocessing}
